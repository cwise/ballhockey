development:
    adapter: postgresql
    host: localhost
    database: ballhockey_prod

test:
    adapter: postgresql
    host: localhost
    database: ballhockey_test

production:
    adapter: postgresql
    host: localhost
    database: ballhockey_prod

